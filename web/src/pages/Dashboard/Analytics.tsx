import React,{useEffect,useState} from 'react';import {getAnalytics} from '../../api/client'
export default function Analytics(){const [a,setA]=useState<any>();useEffect(()=>{getAnalytics().then(setA)},[]);return <div className="dashboard-page"><div className="eyebrow">ANALYTICS</div><h1>University Analytics</h1><div className="panel"><h2>Peak hours</h2>{(a?.hourly||[]).map((x:any)=><div className="bar-row" key={x.hour}><span>{x.hour}:00</span><div><i style={{width:`${Math.min(100,x.count*10)}%`}}/></div><b>{x.count}</b></div>)}</div><div className="panel"><h2>Answer sources</h2>{(a?.sources||[]).map((x:any)=><div className="bar-row" key={x.source}><span>{x.source}</span><div><i style={{width:`${Math.min(100,x.count*10)}%`}}/></div><b>{x.count}</b></div>)}</div></div>}
