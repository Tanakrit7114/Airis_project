import React,{useEffect,useState} from 'react';import {getMemories,getGraph} from '../../api/client'
export default function MemoryExplorer(){const [items,setItems]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{getMemories(q).then(x=>setItems(x.items||[]))},[]);return <div className="dashboard-page"><div className="eyebrow">MEMORY</div><h1>Memory Explorer</h1><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search memory…"/><button onClick={()=>getMemories(q).then(x=>setItems(x.items||[]))}>Search</button></div><div className="memory-grid">{items.map(m=><article className="memory" key={m.memory_id||m.id}><div className="memory-top"><span>{m.memory_type}</span><span>{m.status}</span></div><h3>{m.key}</h3><p>{m.value}</p><div className="scores"><span>Importance {Number(m.importance||0).toFixed(2)}</span><span>Confidence {Number(m.confidence||0).toFixed(2)}</span></div></article>)}</div></div>}
