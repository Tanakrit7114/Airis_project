import React,{useState} from 'react';import Overview from './Overview';import MemoryExplorer from './MemoryExplorer';import Analytics from './Analytics';import Logs from './Logs'
export default function Dashboard(){const [tab,setTab]=useState('overview');return <div className="dashboard-shell"><div className="dash-tabs">{[['overview','Overview'],['memory','Memory'],['analytics','Analytics'],['search-logs','Search logs'],['tool-logs','Tool logs'],['audit-logs','Audit']].map(([id,label])=><button className={tab===id?'active':''} onClick={()=>setTab(id)} key={id}>{label}</button>)}</div>{tab==='overview'?<Overview/>:tab==='memory'?<MemoryExplorer/>:tab==='analytics'?<Analytics/>:<Logs kind={tab}/>}</div>}
