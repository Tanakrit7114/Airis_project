import React,{useState} from 'react'
import MarkdownContent from './MarkdownContent'
import Icon from './Icon'
export default function MessageBubble({m}:{m:any}){const [copied,setCopied]=useState(false);const copy=async()=>{await navigator.clipboard?.writeText(m.content||'');setCopied(true);setTimeout(()=>setCopied(false),1200)};return <div className={'bubble-row '+m.role}><div className="avatar">{m.role==='assistant'?<Icon name="bot" size={15}/>:<Icon name="user" size={15}/>}</div><div className={'bubble '+m.role}><div className="bubble-top"><span>{m.role==='assistant'?'Airis':'You'}</span>{m.created_at&&<time>{new Date(m.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time>}</div><MarkdownContent content={m.content||''}/>{m.role==='assistant'&&m.content&&<button className="copy-btn" onClick={copy} title="Copy response">{copied?'Copied':'Copy'} <Icon name="copy" size={13}/></button>}{m.attachment&&<div className="attachment-mini"><Icon name="file" size={13}/> {m.attachment}</div>}{m.source&&<div className="badge">{m.source==='web'?'Web search':m.source==='file'?'Saved document':m.source==='extension'?'Extension':'Memory / general knowledge'}</div>}{m.sources?.length>0&&<div className="source-list">{m.sources.map((s:any,i:number)=><a key={i} href={s.url} target="_blank" rel="noreferrer" className="source-card"><span className="source-main"><img className="source-card-favicon" src={(() => { try { return new URL(s.url).origin + "/favicon.ico" } catch { return "" } })()} onError={(e)=>{(e.currentTarget as HTMLImageElement).style.display="none"}} alt="" />{s.title||s.url}</span><Icon name="external" size={12}/><small>{s.url}</small></a>)}</div>}{m.persistent_documents?.length>0&&<div className="attachment-mini"><Icon name="file" size={13}/> {Array.from(new Set(m.persistent_documents.map((d:any)=>d.filename))).join(', ')}</div>}</div></div>}
