import React from 'react'
import Icon from './Icon'
type Props={page:string;setPage:(p:string)=>void;sessions:any[];active:string|null;onNew:()=>void;onSelect:(id:string)=>void}
export default function Sidebar({page,setPage,sessions,active,onNew,onSelect}:Props){return <aside className="sidebar"><div className="brand"><div className="orb"><Icon name="zap" size={17}/></div><div><strong>Airis</strong><span>Universal AI</span></div></div><button className="new-chat" onClick={onNew}><Icon name="plus" size={16}/> New chat</button><nav><button className={page==='operator'?'active':''} onClick={()=>setPage('operator')}>Operator</button><button className={page==='chat'?'active':''} onClick={()=>setPage('chat')}><Icon name="message" size={16}/>Chat</button><button className={page==='dashboard'?'active':''} onClick={()=>setPage('dashboard')}><Icon name="dashboard" size={16}/>Dashboard</button><button className={page==='extensions'?'active':''} onClick={()=>setPage('extensions')}><Icon name="plug" size={16}/>Extensions</button></nav>{page==='chat'&&<div className="history"><div className="label">RECENT</div>{sessions.map(s=><button key={s.id} className={active===s.id?'session active':'session'} onClick={()=>onSelect(s.id)}>{s.title}</button>)}</div>}<div className="sidebar-footer">Local-first · Adaptive runtime</div></aside>}
