import React from 'react'
import Icon from './Icon'
const icon=(kind:string)=>kind==='email_list'?<Icon name="mail"/>:kind==='file_list'?<Icon name="folder"/>:kind==='event_list'?<Icon name="calendar"/>:kind==='repo_list'?<Icon name="github"/>:kind==='page_list'?<Icon name="file"/>:<Icon name="chat"/>
export default function ExtensionResult({result}:{result:any}){const items=result?.items||[];return <div className="extension-result"><div className="extension-result-head">{icon(result?.kind)}<strong>ข้อมูลจาก Extension</strong></div>{items.slice(0,8).map((item:any,i:number)=><div className="extension-result-item" key={i}><div className="result-title">{item.name||item.subject||item.summary||item.full_name||item.title||item.id||'รายการ'}</div>{(item.webViewLink||item.html_url||item.url)&&<a href={item.webViewLink||item.html_url||item.url} target="_blank" rel="noreferrer">Open</a>}<small>{item.mimeType||item.email||item.description||item.snippet||item.login||''}</small></div>)}</div>}
